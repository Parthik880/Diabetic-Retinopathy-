.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.cache/
/checkpoint/
/checkpoint_bundle.zip

# Runtime outputs are generated per image and are not persistent results.
inference/outputs/**
!inference/outputs/**/
!inference/outputs/**/.gitkeep

# Datasets stay external to this inference repository.
data/
datasets/
models/lesion/idrid+ddr_lesion.csv

# Trained checkpoints are versioned only in the shared LFS-managed directory.
*.pth
!models/checkpoints/
!models/checkpoints/*.pth
